/*const express = require('express');

const app = express();

const port = 3000;

app.get('/', (req, res) => res.send('hello nodemon!'));
app.listen(port, () => console.log(`Lapplication est demarrée sur: https://localhost:${port}` ))*/
